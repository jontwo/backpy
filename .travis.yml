language: python
python:
    - "2.7"
    - "2.6"

install:
    - "python setup.py install"

script:
    - "python -m unittest -v backpy_tests.config_file"
    - "python -m unittest -v backpy_tests.backup"
    - "python -m unittest -v backpy_tests.restore"
